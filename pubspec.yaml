name: emojsolution
description: Распознавание эмоций людей с аутистическими наклонностями
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4
  camera: ^0.10.5+5
  http: ^1.1.0
  web_socket_channel: ^2.4.0
  video_player: ^2.8.1
  image: ^3.0.1
  auto_size_text: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - config.json